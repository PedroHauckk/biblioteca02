 
        // Função para definir o action do formulário e enviar
        function submitForm(action) {
            document.getElementById('formCliente').action = action;
            document.getElementById('formCliente').submit();
        }
   